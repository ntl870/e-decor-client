import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles({
  head: {
    fontWeight: 500,
    fontSize: 16
  },
  boldText: {
    fontWeight: 500,
  },
  subText:{
    color: "#7D879C"
  }
});
